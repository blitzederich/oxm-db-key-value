# oxm-db-key-value

## Установка

```shell
git clone --recursive git@github.com:blitzederich/oxm-db-key-value.git
```

## Сборка

```shell
mkdir build && cd build
cmake .. 
cmake --build .
```

После запуск тестов:

```shell
./test/tmp_test
```